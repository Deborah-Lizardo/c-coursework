#include <stdio.h>

int main () {
    int n;
    int result = 0;
    int factorial = 0;

    printf("Please input the factorial number you want to calculate: \n");
    scanf("%i", &n);

    for (n != i; i ++, n--){
        printf("factorial %i %i\n", n, i);
        factorial = n - i ;
        factorial = n * i; //
        result = result+ factorial;
        printf(The result is: %i\n", result);
    }"
    return 0;
}