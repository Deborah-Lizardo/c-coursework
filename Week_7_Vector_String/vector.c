#include <stdio.h>

int main() {
    int numbers[5] = {1, 2, 3, 4, 5};  // Array initialized

    // Reading specific element
    printf("The third element is: %d\n", numbers[2]);  // Index 2 corresponds to the third element (3)

    return 0;
}
